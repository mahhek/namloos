
<h1>Users</h1>

<table>
  
  <h3>Email</h3>
    
<% @users.each do |site_user| %>
  <tr>
    <td><%= site_user.email %></td>
    <td><%= link_to 'Show', "/site_users/show/#{site_user.id}" %></td>
    <td><%= link_to 'Edit', edit_site_user_path(site_user) %></td>
    <td><%= link_to 'Destroy', "site_users/destroy/#{site_user.id}", :confirm => 'Are you sure?', :method => :delete %></td>
    <td><%= link_to 'Assign Privileges', "site_users/assign_privileges/#{site_user.id}" %></td>
  </tr>
<% end %>
</table>

<br />
<%= link_to 'New User', new_site_user_path %>