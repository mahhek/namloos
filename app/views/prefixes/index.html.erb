<h1>Prefixes</h1>

<table>
  
    <h3>Codes:</h3>
    <th></th>
    <th></th>
    <th></th>
  

<% @prefixes.each do |prefix| %>
  <tr>
    <td><%= prefix.name %></td>
    <td><%= link_to 'Show', prefix %></td>
    <td><%= link_to 'Edit', edit_prefix_path(prefix) %></td>
    <td><%= link_to 'Destroy', prefix, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Prefix', new_prefix_path %>
