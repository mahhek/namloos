<%= form_for(@prefix) do |f| %>
  <% if @prefix.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@prefix.errors.count, "error") %> prohibited this prefix from being saved:</h2>

      <ul>
      <% @prefix.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
