<%= form_for(@call_defination) do |f| %>
  <% if @call_defination.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@call_defination.errors.count, "error") %> prohibited this call_defination from being saved:</h2>

      <ul>
      <% @call_defination.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
