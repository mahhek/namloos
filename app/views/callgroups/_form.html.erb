<%= form_for(@callgroup) do |f| %>
  <% if @callgroup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@callgroup.errors.count, "error") %> prohibited this callgroup from being saved:</h2>

      <ul>
      <% @callgroup.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
